import { useEffect, useState } from "react";
import { KpiCard, PageHeader } from "../components/analytics/DashboardComponents";

type EvaluationResult = { generated_at: string; model: string; metrics: { evaluation_cases: number; completed_cases: number; service_unavailable_cases: number; tool_selection_accuracy: number | null; required_tool_usage_rate: number | null; numeric_fact_match_rate: number | null; average_execution_time_ms: number | null }; category_counts: Record<string, number> };
const architecture = ["User Request", "Gemini Agent", "Allow-listed Tool Registry", "Deterministic Analytics", "Synthetic Business Data", "Structured Tool Results", "Grounded Final Response"];
const tools = [{ title: "Sales", text: "Summaries, product rankings, period comparisons, product performance, and daily or monthly trends." }, { title: "Inventory", text: "Centralized status thresholds, at-risk ordering, catalog detail, and portfolio-level health." }, { title: "Suppliers", text: "Supplier detail, product coverage, lead time, reliability, and current inventory exposure." }, { title: "Reorder", text: "Multi-source prioritization combining stock severity, recent demand, lead time, and reliability." }];

export function MethodologyPage() {
  const [evaluation, setEvaluation] = useState<EvaluationResult | null>(null);
  useEffect(() => { fetch("/evaluation/agent_evaluation_latest.json").then((response) => response.ok ? response.json() : Promise.reject()).then(setEvaluation).catch(() => setEvaluation(null)); }, []);
  return <section className="space-y-8"><PageHeader eyebrow="Research design and reproducibility" title="Methodology / Evaluation" description="How deterministic analytics, validated tool calling, and repeatable benchmarks create a grounded business assistant." />
    <section className="grid gap-5 lg:grid-cols-[0.9fr_1.1fr]"><article className="rounded-xl border border-slate-200 bg-white p-6 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Problem Definition</p><h3 className="mt-2 text-xl font-semibold text-slate-900">Grounded assistance, not generated business facts</h3><p className="mt-4 text-sm leading-7 text-slate-600">This portfolio project demonstrates an AI business process assistant that combines deterministic analytics, LLM tool selection, and business-intelligence visualization. Gemini interprets intent and communicates results; Python tools remain the source of quantitative business facts.</p></article><article className="rounded-xl border border-slate-200 bg-slate-950 p-6 text-white shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">System Architecture</p><div className="mt-5 grid gap-2 sm:grid-cols-2 xl:grid-cols-4">{architecture.map((step, index) => <div key={step} className="relative rounded-lg border border-slate-700 bg-slate-900 p-3"><p className="text-[10px] font-semibold text-slate-500">STEP {index + 1}</p><p className="mt-1 text-sm font-semibold">{step}</p>{index < architecture.length - 1 ? <span className="absolute -bottom-2 left-1/2 text-slate-500 sm:-right-2 sm:bottom-auto sm:left-auto sm:top-1/2">↓</span> : null}</div>)}</div></article></section>
    <section><div className="mb-4"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Deterministic Tool Layer</p><h3 className="mt-1 text-xl font-semibold text-slate-900">Testable analytics before model interpretation</h3></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{tools.map((tool) => <article key={tool.title} className="rounded-xl border border-slate-200 bg-white p-5 shadow-card"><h4 className="font-semibold text-slate-900">{tool.title} tools</h4><p className="mt-2 text-sm leading-6 text-slate-600">{tool.text}</p></article>)}</div></section>
    <section className="grid gap-6 xl:grid-cols-2"><article className="rounded-xl border border-slate-200 bg-white p-6 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Grounding Strategy</p><h3 className="mt-2 text-xl font-semibold text-slate-900">A constrained read-only boundary</h3><ul className="mt-4 space-y-3 text-sm leading-6 text-slate-600">{["Gemini cannot read CSV files or environment variables directly.","Quantitative claims must come from deterministic tool results.","Tool names and parameters are allow-listed and validated.","Unknown tools and unexpected arguments are rejected.","Tool execution is bounded to five rounds per request.","The assistant has no write, purchasing, email, or inventory-modification tools."].map((item) => <li key={item} className="flex gap-3"><span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-accent" aria-hidden="true" />{item}</li>)}</ul></article>
      <article className="rounded-xl border border-slate-200 bg-white p-6 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Reorder Priority Methodology</p><h3 className="mt-2 text-xl font-semibold text-slate-900">Transparent score, maximum 100</h3><div className="mt-5 space-y-3">{[["Status urgency","50 Critical / 25 Low Stock",50],["Shortage severity","Stock ratio deficit",20],["Recent demand","Trailing 30-day units",15],["Supplier lead time","Normalized lead time",10],["Reliability risk","One minus reliability",5]].map(([label, description, points]) => <div key={String(label)}><div className="flex justify-between text-sm"><span className="font-medium text-slate-700">{label}</span><span className="font-semibold text-slate-900">Up to {points} pts</span></div><p className="mt-0.5 text-xs text-slate-500">{description}</p><div className="mt-2 h-1.5 rounded-full bg-slate-100"><div className="h-1.5 rounded-full bg-accent" style={{ width: `${Number(points)}%` }} /></div></div>)}</div><p className="mt-5 text-sm leading-6 text-slate-600">The score is intentionally explainable and illustrative. It is deterministic—not learned by a model—and every recommendation exposes its component evidence.</p></article></section>
    <section className="rounded-xl border border-slate-200 bg-white p-6 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Synthetic Dataset</p><div className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><KpiCard label="Sales Transactions" value="2,000" /><KpiCard label="Products" value="40" /><KpiCard label="Suppliers" value="10" /><KpiCard label="Coverage" value="12 months" /><KpiCard label="Reference Date" value="2025-12-31" /></div><p className="mt-4 text-sm text-slate-500">All company, customer, product, and supplier records are synthetic and reproducible from a fixed random seed.</p></section>
    <section className="rounded-xl border border-slate-200 bg-white p-6 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Agent Evaluation</p><h3 className="mt-2 text-xl font-semibold text-slate-900">Repeatable tool-selection benchmark</h3><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-600">Twenty fixed prompts cover sales, inventory, suppliers, reorder decisions, multi-tool questions, capability questions, and unsupported actions. Exact tool sets are evaluated without grading natural-language style. Gemini availability and quota failures are tracked separately from logical failures.</p>{evaluation ? <><div className="mt-5 rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-950"><p className="font-semibold">Live benchmark status: {evaluation.metrics.completed_cases} of {evaluation.metrics.evaluation_cases} cases completed before free-tier quota exhaustion.</p><p className="mt-1 text-amber-800">Completed-case metrics below have a sample size of n={evaluation.metrics.completed_cases}; they are not a full model-quality result.</p></div><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><KpiCard label="Completed Cases" value={`${evaluation.metrics.completed_cases} / ${evaluation.metrics.evaluation_cases}`} detail="Quota-limited live run" /><KpiCard label="Tool Selection" value={evaluation.metrics.tool_selection_accuracy === null ? "N/A" : `${evaluation.metrics.tool_selection_accuracy.toFixed(1)}%`} detail={`Completed cases · n=${evaluation.metrics.completed_cases}`} /><KpiCard label="Required Tool Usage" value={evaluation.metrics.required_tool_usage_rate === null ? "N/A" : `${evaluation.metrics.required_tool_usage_rate.toFixed(1)}%`} detail={`Completed cases · n=${evaluation.metrics.completed_cases}`} /><KpiCard label="Numeric Grounding" value={evaluation.metrics.numeric_fact_match_rate === null ? "N/A" : `${evaluation.metrics.numeric_fact_match_rate.toFixed(1)}%`} detail={`Completed cases · n=${evaluation.metrics.completed_cases}`} /></div><div className="mt-5 flex flex-wrap gap-2">{Object.entries(evaluation.category_counts).map(([category, count]) => <span key={category} className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-medium text-slate-600">{category.replaceAll("_", " ")} · {count}</span>)}</div><p className="mt-4 text-xs text-slate-400">Generated {new Date(evaluation.generated_at).toLocaleString()} with {evaluation.model}. Average completed-case response time: {evaluation.metrics.average_execution_time_ms === null ? "N/A" : `${(evaluation.metrics.average_execution_time_ms / 1000).toFixed(1)} s`}.</p></> : <div className="mt-5 rounded-lg border border-dashed border-slate-300 bg-slate-50 p-6 text-sm text-slate-600">Live evaluation not yet run.</div>}</section>
    <section className="grid gap-6 xl:grid-cols-2"><article className="rounded-xl border border-slate-200 bg-white p-6 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Limitations</p><ul className="mt-4 grid gap-2 text-sm leading-6 text-slate-600">{["Synthetic rather than operational data","No persistent multi-user memory","No write or action tools","No real ERP integration","Gemini availability and latency may vary","Illustrative deterministic score weights","No production authentication or security model"].map((item) => <li key={item}>— {item}</li>)}</ul></article><article className="rounded-xl border border-slate-200 bg-white p-6 shadow-card"><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Reproducibility</p><ul className="mt-4 grid gap-2 text-sm leading-6 text-slate-600">{["Fixed synthetic-data seed","Deterministic analytics and score formula","Pytest calculation and integration coverage","Known dataset reference date","Versioned, repeatable benchmark prompts","Offline schema, registry, and fact validation"].map((item) => <li key={item}>— {item}</li>)}</ul></article></section>
  </section>;
}
